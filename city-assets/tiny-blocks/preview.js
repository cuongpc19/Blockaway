import * as THREE from '../../vendor/three.module.js';
import {GLTFLoader} from '../vendor/GLTFLoader.js';
const c=document.querySelector('#view'),r=new THREE.WebGLRenderer({canvas:c,antialias:true,alpha:true,preserveDrawingBuffer:true});r.setPixelRatio(Math.min(devicePixelRatio||1,2));r.outputColorSpace=THREE.SRGBColorSpace;
const s=new THREE.Scene(),cam=new THREE.PerspectiveCamera(34,1,.1,100);s.add(new THREE.HemisphereLight(0xffffff,0x637660,2));const l=new THREE.DirectionalLight(0xfff2d8,3);l.position.set(-4,8,7);s.add(l);const g=new THREE.Mesh(new THREE.PlaneGeometry(100,100),new THREE.MeshStandardMaterial({color:0x83a47e}));g.rotation.x=-Math.PI/2;g.position.y=-.08;g.receiveShadow=true;s.add(g);
let object=null,yaw=-.7;const loader=new GLTFLoader();loader.load('./tiny-block-kit.glb',x=>{object=x.scene;s.add(object);const b=new THREE.Box3().setFromObject(object),z=b.getSize(new THREE.Vector3());const f=b.getCenter(new THREE.Vector3());cam.position.set(f.x+6,f.y+5,f.z+8);cam.lookAt(f);});
function resize(){const b=c.getBoundingClientRect();r.setSize(b.width,b.height,false);cam.aspect=b.width/b.height;cam.updateProjectionMatrix();}new ResizeObserver(resize).observe(c);resize();function tick(){requestAnimationFrame(tick);if(object){yaw+=.002;object.rotation.y=yaw;const b=new THREE.Box3().setFromObject(object),f=b.getCenter(new THREE.Vector3());cam.position.set(f.x+6,f.y+5,f.z+8);cam.lookAt(f);}r.render(s,cam);}tick();
